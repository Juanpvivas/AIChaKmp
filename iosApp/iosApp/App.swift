import SwiftUI

@main
struct AIChaApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}

struct ContentView: View {
    var body: some View {
        Text("AICha — KMP placeholder")
            .padding()
    }
}
